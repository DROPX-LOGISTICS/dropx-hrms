import type { Metadata } from "next";
import { AppShell } from "@/components/app-shell";
import { PageHeader } from "@/components/page-header";
import { StatusPill } from "@/components/status-pill";
import { requireHrmsAuth } from "@/lib/auth";
import { listLeaveTypes, loadCompanySettings } from "@/lib/data";
import { addLeaveType, saveCompanySettings, toggleLeaveType } from "./actions";

export const metadata: Metadata = { title: "Settings" };
export const dynamic = "force-dynamic";
const week = [{ value: "mon", label: "Mon" }, { value: "tue", label: "Tue" }, { value: "wed", label: "Wed" }, { value: "thu", label: "Thu" }, { value: "fri", label: "Fri" }, { value: "sat", label: "Sat" }, { value: "sun", label: "Sun" }];

export default async function SettingsPage({ searchParams }: { searchParams?: { error?: string; notice?: string } }) {
  const auth = await requireHrmsAuth("settings.manage");
  const [settings, leaveTypes] = await Promise.all([loadCompanySettings(auth), listLeaveTypes(auth, true)]);
  const workWeek = new Set<string>(settings?.work_week ?? ["mon", "tue", "wed", "thu", "fri", "sat"]);
  return <AppShell auth={auth} active="Settings"><PageHeader eyebrow="HR policy" title="Settings" description="Company-level attendance and leave configuration." />{searchParams?.error ? <div className="alert error">{searchParams.error}</div> : null}{searchParams?.notice ? <div className="alert success">{searchParams.notice}</div> : null}<section className="grid two-column"><article className="panel"><div className="panel-head"><h2>Attendance policy</h2></div><div className="panel-body"><form action={saveCompanySettings}><div className="field"><label>Working days</label><div className="inline-actions">{week.map((day) => <label key={day.value} className="button secondary small"><input type="checkbox" name="work_week" value={day.value} defaultChecked={workWeek.has(day.value)} /> {day.label}</label>)}</div></div><div className="form-grid" style={{ marginTop: 16 }}><div className="field"><label htmlFor="attendance_grace_minutes">Grace period (minutes)</label><input id="attendance_grace_minutes" name="attendance_grace_minutes" type="number" min="0" max="180" defaultValue={settings?.attendance_grace_minutes ?? 15} required /></div><div className="field"><label htmlFor="full_day_minutes">Full day (minutes)</label><input id="full_day_minutes" name="full_day_minutes" type="number" min="60" defaultValue={settings?.full_day_minutes ?? 480} required /></div><div className="field"><label htmlFor="half_day_minutes">Half day (minutes)</label><input id="half_day_minutes" name="half_day_minutes" type="number" min="30" defaultValue={settings?.half_day_minutes ?? 240} required /></div><div className="field"><label htmlFor="leave_year_start_month">Leave year starts</label><select id="leave_year_start_month" name="leave_year_start_month" defaultValue={settings?.leave_year_start_month ?? 1}>{Array.from({ length: 12 }, (_, index) => <option key={index + 1} value={index + 1}>{new Intl.DateTimeFormat("en", { month: "long" }).format(new Date(2026, index, 1))}</option>)}</select></div></div><div className="form-actions"><button className="button primary" type="submit">Save policy</button></div></form></div></article><article className="panel"><div className="panel-head"><h2>Add leave type</h2></div><div className="panel-body"><form action={addLeaveType}><div className="grid"><div className="field"><label htmlFor="name">Name</label><input id="name" name="name" required /></div><div className="field"><label htmlFor="code">Code</label><input id="code" name="code" placeholder="CASUAL" required /></div><div className="field"><label htmlFor="annual_allowance">Annual allowance</label><input id="annual_allowance" name="annual_allowance" type="number" min="0" max="365" required /></div><div className="field"><label htmlFor="color">Colour</label><input id="color" name="color" type="color" defaultValue="#1f7a50" /></div><button className="button primary" type="submit">Create leave type</button></div></form></div></article></section><section className="panel" style={{ marginTop: 18 }}><div className="panel-head"><h2>Leave types</h2></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Code</th><th>Annual allowance</th><th>Status</th><th>Action</th></tr></thead><tbody>{leaveTypes.length ? leaveTypes.map((type) => <tr key={type.id}><td><strong>{type.name}</strong></td><td>{type.code}</td><td>{type.annual_allowance} days</td><td><StatusPill value={type.is_active ? "active" : "inactive"} /></td><td><form action={toggleLeaveType}><input type="hidden" name="leave_type_id" value={type.id} /><input type="hidden" name="next_active" value={type.is_active ? "false" : "true"} /><button className="button secondary small" type="submit">{type.is_active ? "Deactivate" : "Activate"}</button></form></td></tr>) : <tr><td className="empty-cell" colSpan={5}>No leave types configured.</td></tr>}</tbody></table></div></section></AppShell>;
}
